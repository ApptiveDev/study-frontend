function Header(props) {
    return(
        <header>
            <p>{props.title}</p>
        </header>
    );
}

export default Header;

// import Header from 'Header';

// import header from 'header';
// header.header
